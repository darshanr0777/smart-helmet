# Workspace Guidelines

## Git Workflow
- Whenever changes are made to the codebase or features are completed, always stage, commit with a descriptive message, and push the changes to the remote repository (`git push origin main`).
